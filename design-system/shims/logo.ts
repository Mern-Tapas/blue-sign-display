// Stand-in for `@/public/logo.jpg`: the same artwork (public/logo.jpg) scaled to 128px so it
// can be inlined into the bundle. Regenerate from public/logo.jpg if the logo ever changes.
import src from "./logo-128.png";

const logo = { src, width: 128, height: 128 };
export default logo;
